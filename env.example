AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret
AUTH_SECRET=generate-a-random-secret-here
# Generate a secret with: npx auth secret

# Database Configuration
# IMPORTANT: Use POSTGRES_URL_DEV for local development to separate dev and prod databases
# This prevents test data from appearing in production
POSTGRES_URL_DEV=your_dev_database_connection_string

# POSTGRES_URL is used in production (set in Vercel Dashboard → Environment Variables)
# For local dev, this is only used as a fallback if POSTGRES_URL_DEV is not set
POSTGRES_URL=your_production_database_connection_string
