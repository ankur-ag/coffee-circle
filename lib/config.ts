/**
 * Global configuration constants
 */

/**
 * Number of days before the event when the location should be revealed
 * Change this value to adjust when locations become visible to users
 */
export const LOCATION_REVEAL_DAYS: number = 2;
